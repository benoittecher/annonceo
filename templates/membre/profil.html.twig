{% extends "menu/menu_connecte.html.twig" %}
{% block body %}
    <h1>Profil</h1>
    <div class="columns is-centered section">
        <div class="column is-half">
            <p>
            {% if app.user.civilite == "H" %}
                Monsieur
            {% elseif app.user.civilite == "F"%}
                Madame
            {% else %}
                
            
            {% endif %}
            {{ app.user.nom | upper }}{{   " " ~ app.user.prenom}}</p>
            <p>{{ app.user.pseudo }}</p>
            <p>{{ app.user.telephone }}</p>
            <p>{{ app.user.email }}</p>
            <p>Inscrit{{app.user.civilite == "F" ? "e" : ""}} depuis le {{ app.user.dateenregistrement|date("d/m/Y à H:i:s") }}</p>
        </div>
        <div class="column is-half">
            {% for annonce in mes_annonces %}
                {% include "annonce/vignette.html.twig" with { "annonce": annonce } %}
            {% endfor %}

        </div>
    </div>
{% endblock %}